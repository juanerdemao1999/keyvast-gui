[package]
name = "kv-recorder"
version = "0.1.0"
edition.workspace = true

[dependencies]
kv-types = { path = "../kv-types" }
log.workspace = true

[dev-dependencies]
kv-simulator = { path = "../kv-simulator" }
